import Lenis from "@studio-freight/lenis";
import {gsap} from "gsap";
import {useEffect,type RefObject} from "react";
export function useLandingMotion(ref:RefObject<HTMLElement|null>){useEffect(()=>{const root=ref.current;if(!root||window.matchMedia("(prefers-reduced-motion: reduce)").matches)return;const lenis=new Lenis({duration:1.05,smoothWheel:true,wheelMultiplier:.8,touchMultiplier:1.15});let frame=0;const run=(time:number)=>{lenis.raf(time);frame=requestAnimationFrame(run)};frame=requestAnimationFrame(run);const context=gsap.context(()=>{gsap.fromTo("[data-hero]",{y:24},{y:0,duration:.86,stagger:.1,ease:"power3.out",delay:.08});const targets=Array.from(root.querySelectorAll<HTMLElement>("[data-reveal]"));gsap.set(targets,{autoAlpha:0,y:32});const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;gsap.to(entry.target,{autoAlpha:1,y:0,duration:.8,ease:"power3.out"});observer.unobserve(entry.target)}),{threshold:.14});targets.forEach(node=>observer.observe(node));return()=>observer.disconnect()},root);return()=>{cancelAnimationFrame(frame);lenis.destroy();context.revert()}},[ref])}
