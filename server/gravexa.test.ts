import {describe,expect,it,vi} from "vitest";
import {explainResult,explanationInput,explanationOutput,findReference,referenceInput} from "./gravexa";
describe("Gravexa server input",()=>{it("rejects impossible explanation values",()=>{expect(explanationInput.safeParse({status:"clear",closestDistanceKm:-1,safetyMarginKm:1,speedKmS:2,uncertaintyKm:0,asteroidDiameterM:5}).success).toBe(false)});it("rejects unsafe JPL query text",()=>{expect(referenceInput.safeParse({query:"Bennu<script>"}).success).toBe(false)});it("accepts the explanation response shape",()=>{expect(explanationOutput.safeParse({title:"Clear path",summary:"The shown path remains clear.",whatChanged:"The path stays away from Earth.",note:"This is a local learning model."}).success).toBe(true)});it("fails safely when the Groq key is missing",async()=>{const original=process.env.GROQ_API_KEY;delete process.env.GROQ_API_KEY;await expect(explainResult({status:"clear",closestDistanceKm:20000,safetyMarginKm:12000,speedKmS:10,uncertaintyKm:500,asteroidDiameterM:300})).rejects.toThrow("not available");process.env.GROQ_API_KEY=original});it("fails safely when JPL does not return a response",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:false}));await expect(findReference("Bennu")).rejects.toThrow("did not complete");vi.unstubAllGlobals()})});
